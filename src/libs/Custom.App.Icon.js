import {createIconSetFromFontello} from 'react-native-vector-icons';
import fontelloConfig from '../../assets/fonts/config.json';

export const CustomAppIcon = createIconSetFromFontello(fontelloConfig);
